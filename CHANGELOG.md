# 更新日志

应用与词包**各自独立发版**，互不同步：应用更新了词包可以不动，词包更新了应用也不必跟着升级。
所以下面分两节，各自按自己的版本号从新到旧记。

版本号规则：

- **应用** `v主.次.修订` —— 功能、界面改动时递增。
- **词包** `v序号` —— 每次重建数据递增。词包 ID 恒为 `cet46`，换包不丢学习进度。

---

## 应用

### v4.1.0

- 修正温习功能的判定逻辑：点「显示完整答案（核对用）」核对释义后，仍由用户自己选「记住了 / 没记住」；只有走完「逐条展示例句 → 显示例句意思」这条路才会自动记为没记住。
- 温习卡片上看过答案之后，逐条翻出的例句会补上中文译文，方便对着原文核对。
- 词本列表与词本详情显示词包版本号；设置里同时列出应用版本与词包版本。

### v4.0.0

- 首个公开版本：识记 → 温习一 → 温习二 → 最终页面的完整学习链路。
- 释义按来源分组标注「真题」与「大纲补充」；例句、短语搭配取自四六级真题原文与官方译文。
- 全离线运行，不申请网络权限；音频随词包导入，无需联网。

---

## 词包

### v12

- 例句去重：删除同一词条内「同一句话被切成长短两条」的重复例句，共 1692 条。
- 删除译文被截断的残缺例句 135 条；这类例句的中文停在半句上，无法核对。
- 重建义项来源标注：改用「真题译文里的实际用词」作依据，一批真题里确实出现过的
  义项由「大纲补充」改判为「真题」（含 ago、make、give、iron、address 等高频词）。
- 例句总数 39687 → 37860。

### v11

- 重建短语搭配：只保留在至少两套真题里复现过的搭配。
- 收窄形近词与近义词的收录范围；补充真题原文里出现过的单词变形。
- 音标改用 CMUdict，逐个核对，收不到的不臆造。
